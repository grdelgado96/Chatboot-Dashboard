service: my-next-app

provider:
  name: aws
  deploymentMethod: direct
  runtime: nodejs18.x
  region: eu-central-1

functions:
  # Aquí puedes definir funciones Lambda si las necesitas

plugins:
  #- serverless-s3-deploy
  - serverless-finch

custom:
  # assets:
  #   #auto: true
  #   targets:
  #      - bucket: chatbot-demo-bucket
  #        prefix: subdir
  #        files:
  #         - source: ../out/
  client:
    bucketName: chatbot-demo-bucket
    distributionFolder: /out  
    region: eu-central-1